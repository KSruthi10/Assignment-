#include<bits/stdc++.h>
using namespace std;
int maxConsecutiveOnes(int N)
{
    int count = 0;
    while (N!=0)
    {
        count++;
    }
    return count;
}
int main()
{
    int x;
    cin>>x;
    cout << maxConsecutiveOnes(x) << endl;
    return 0;
}
